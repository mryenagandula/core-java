package Interfaces.Example1;

/** DESIGN */
public interface ProjectApp {
    /**
     * In Interfaces we declared variables. Those variable will act like final and
     * static
     * Meaning we can't able to update the variable value.and even we can't able to
     * add construcors in
     * Interfaces due to interfaces no constructors.
     */
    int a = 10;

    /**
     * In Interfaces we declared methods and those methods will act like abstract
     * methods
     * Meaning methods not containing body and implementaion and interface all
     * methods
     * Are abstract methods, Even we can't able to write normal methods and
     * staticmethods
     */
    void task1();

    void task2();
}
